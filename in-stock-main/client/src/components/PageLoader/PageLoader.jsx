import './PageLoader.scss';

function PageLoader () {
    return (
        <div className="page-loader">
            <h2 className="page-loader__title">Loading...</h2>
        </div>
    );
}

export default PageLoader;